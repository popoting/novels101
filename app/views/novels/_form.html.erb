<%= simple_form_for @novel do |f| %>
  <div class="form-group">
    <%= f.input :title %>
    <%= f.input :description %>
    
    <%= f.input :author %>
    <%= f.input :lead %>
    
  </div>
  <%= f.submit "Submit", class: "btn btn-primary", data: { disable_with: "Submiting..." } %>
<% end %>
