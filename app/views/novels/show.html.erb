<div class="group">
  <% if current_user && current_user == @novel.user %>
  <%= link_to("Edit", edit_novel_path(@novel), class: "btn btn-outline-primary float-right") %>
  <% end %>
</div>
<h2><%= @novel.title %></h2>
<div><%= render @novel.tags %></div>
<p> 作者：<%= @novel.author %></p>
<p>简介：
   <br>
  <%= @novel.description %></p>
<p>
  主角：<%= @novel.lead %></p>