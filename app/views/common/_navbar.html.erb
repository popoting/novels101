<nav class="navbar navbar-expand-lg navbar-light fixed-top" style="background-color: #e3f2fd;">
  <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
    <a class="navbar-brand" href="/">Novel101</a>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <a class="nav-link" href="#">最热门 <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">最多收藏</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">最新发布</a>
      </li>
    </ul>
    <form class="form-line my-2 my-lg-0" >
      <%= search_form_for @search, url: search_novels_path, html: {method: :post, class: "navbar-form"} do |f| %>
    <p>
      <%= f.text_field :title_cont, class: "form-control" %>
      <%= f.submit "Search", class: "btn btn-default" %>
    </p>
  <% end %>
    </form>
      
    <% if !current_user %>
      <li><%= link_to("注册", new_user_registration_path) %> </li>
      <li><%= link_to("登录", new_user_session_path) %></li>
    <% else %>
      <li class="dropdown">
       <a href="#" class="dropdown-toggle btn btn-success" data-toggle="dropdown">
            Hi!, <%= current_user.email %>
        <b class="caret"></b>
        </a>
       <ul class="dropdown-menu">
        <li class="dropdown-item"> <%= link_to("退出", destroy_user_session_path, method: :delete) %> </li>
       </ul>
      </li>
    <% end %>

</nav>